## 更新计划：README.md 项目结构和功能文档优化

### 需要更新的内容

1. **项目结构部分**

   * 移除 `apps/web/` 前缀（实际文件直接在根目录）

   * 更新 `components/` 结构，添加新组件

   * 更新 `lib/generator/` 结构，添加 `export.ts`

2. **功能特性部分**

   * 添加 React/Vue 组件导出功能说明

   * 更新导出格式列表

3. **技术栈部分**

   * 添加新的依赖库说明

### 修改文件

* `README.md`

### 具体修改

| 位置                | 当前内容                               | 更新为                                                                          |
| ----------------- | ---------------------------------- | ---------------------------------------------------------------------------- |
| 项目结构              | `apps/web/`                        | 移除前缀，直接显示 `app/`, `components/`                                              |
| lib/generator     | 只有 index, color, shadow, normalize | 添加 export.ts                                                                 |
| components/output | 未提及                                | 添加 CodeBlock, CodePreview, CodeTabs, ExportButtons, ExportModal, OutputPanel |
| 功能特性              | CSS/Tailwind/HTML/JSON             | 添加 React/Vue 组件导出                                                            |

